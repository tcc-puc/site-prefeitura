version: "3"

services:
  nextjs:
    ports:
      - 3033:3033
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/.next